# 08 - Unresolved Questions

| Question | Why it matters | Current evidence | Blocking? | Recommended action |
|---|---|---|---|---|
| Is result-bearing B1 supposed to be `none_mode=predictor` or `none_mode=b1`? | The scripts and directory names call `predictor` runs B1, while code enum `b1` means predictor frame plus physical remap. | `scripts/run_per_head_order_scan_ladder_b1.sh` uses `--none-mode predictor`; `train_clean_aogpt.py` default for tracker is `b1`; latest config records `track_head_none_mode=predictor`. | Yes for paper-method precision. | Add a one-paragraph convention note, or rerun a small matched scan with `none_mode=b1` if physical-remapped B1 is intended. |
| Has a B1 frozen hook run been completed? | Needed to claim B1-controller acceleration. | No B1 hook result found; hook code uses `_attn_to_A_block_b0_vec`. | Blocking only for B1 acceleration claim. | Do not claim B1 acceleration. Optional B1 hook rerun if method switches controller to B1. |
| Has B1 g_beta sanity been run? | Needed to claim g_beta reads B1 graphs rather than B0 graphs. | Existing sanity JSON uses B0 selected-head dataset paths. Source: `reports/evidence_package_20260613_verified/raw/gbeta_input_sanity_final.json`. | Blocking for B1 g_beta claim. | Optional B1 g_beta sanity if controller evidence must be post-alignment. |
| Is there a B1 shuffled-L2R control? | Needed to exclude physical-order leakage under B1. | No B1 shuffled-L2R scan found. | Important for evidence package. | Run or locate B1 shuffled-L2R diagnostic; until then use B0 shuffled control as legacy evidence only. |
| Is there a B1 50k head scan for seed-123 continuous? | Needed to align with previous seed-123 50k B0 attenuation claim. | B0 50k exists: `random_baseline_continuous_jun08_seed2/head_scan_50k.json`; no B1 counterpart found. | Not blocking for "signal survives" because seed124 B1 exists, but blocks matched comparison. | Run B1/predictor scan on seed-123 continuous 50k if exact comparison matters. |
| Is there a B1 scan for seed-42 continuous selected head L0H4? | Needed to connect B1 to the seed-42 frozen_beta group. | No B1 scan found for `random_baseline_continuous_jun05`. | Blocking for seed-42 B1-controller narrative. | Run B1/predictor scan on seed-42 checkpoints if needed. |
| Is there a B1 317M scan? | Needed to move scale diagnostic from B0 legacy to B1 post-alignment. | Only B0 317M file found: `large_random_baseline_16l16h1024d/head_scan_step5000.json`. | Not blocking for small-model B1 claim; blocks B1 scale claim. | Optional B1 317M scan. |
| Does B1 change selected head? | Head indices can change across extraction protocols; controller must train and serve on the same extraction/head. | clean-base B1 best is L0H0; continuous seed124 B1 best is L0H4. Existing B0 controllers use seed-123 L0H2 and seed-42 L0H4. | Important for hook consistency. | If rerunning B1 controller, select heads under B1 and train g_beta on the same B1 head used online. |
| Does B1 change strong-head count? | Sparse vs dense signal characterization depends on count. | clean-base B0 @10k has `|tau|>0.9`=9; clean-base B1 @10k has 7. Continuous seed124 B1 final has 1. | Not blocking. | Report count per protocol and do not overstate density. |
| Does B1 affect CDL teacher ordering? | CDL teacher consumes `B`; changing B changes teacher labels. | B1 scans still use `generate_teacher_label(B)` and show high tau. No B1 controller dataset found. | Blocking only for B1 controller training target. | If B1 hook is pursued, rebuild g_beta datasets from B1 B and re-evaluate teacher diversity. |
| Does B1 affect g_beta training target? | Existing g_beta target is CDL labels from B0 selected-head B. | `selected_head_dataset.py` and phase reports use `none_mode="b0"`. | Yes for B1 controller claim. | Keep existing g_beta as B0 legacy; build new B1 dataset only if needed. |
| Is external collaborator implementation present for direct parity check? | Needed to claim exact implementation alignment. | No external collaborator extraction script found in requested directories. | Blocking for "bit-for-bit collaborator parity" wording. | Use cautious wording: collaborator-aligned convention, not verified external-code parity. |

